import 'package:flutter/material.dart';

class TextFieldDropDownWidget extends StatelessWidget {
  // final String? errorText;
  const TextFieldDropDownWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
